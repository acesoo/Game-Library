# nixpacks.toml
# Tells Railway how to build and run this PHP project

[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mbstring", "php82Extensions.fileinfo"]

[start]
cmd = "php -S 0.0.0.0:$PORT"
